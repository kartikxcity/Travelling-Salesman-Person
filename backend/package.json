{
  "name": "tsp-backend-bridge",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "compile:cpp": "if not exist build mkdir build && g++ -std=c++17 -O2 cpp_runner.cpp -o build/tsp_runner.exe",
    "start:cpp": "npm run compile:cpp && set TSP_ENGINE=cpp&& node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2"
  }
}
